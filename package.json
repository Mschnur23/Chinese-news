{
  "name": "daily-chinese-read",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-guidelines.mjs && node --test tests/*.test.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
